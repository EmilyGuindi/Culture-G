{
  "name": "culture-g-daily",
  "version": "1.0.0",
  "private": true,
  "description": "Culture G Daily — app mobile de culture générale (5 min/jour).",
  "scripts": {
    "build:web": "node scripts/build-web.mjs",
    "sync": "npm run build:web && cap sync android",
    "android:add": "npm run build:web && cap add android",
    "android:apk": "npm run android:add && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
